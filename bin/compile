#!/bin/bash

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

echo '==================== INSTALLING MJML ===================='

cd $BUILD_DIR

/app/.heroku/node/bin/npm install -g mjml

echo "Exit succesfully regardless of logging"